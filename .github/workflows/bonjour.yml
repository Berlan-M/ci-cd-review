name: Bonjour
on: [push]
jobs:
  dire_bonjour:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - run: ls -l # Liste les fichier du répertoire courant
    - run: cat README.md